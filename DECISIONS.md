# FlowForge — Spec Deviations

Every place the build diverged from PHASE2.md, one line each: what changed, why, when. Append, don't rewrite.

- **cron-parser v5, not v4** — M1 approved `cron-parser` as a dependency for cron validation in `job-config.schema.ts`. The version tentatively picked (v4.9.0) is marked deprecated upstream ("no longer maintained, upgrade to v5"), so v5 was used instead — same library, current major. 2026-08-11.
- **Hosted Neon + Upstash, not local Docker, as the default in SETUP.md** — §13 asked which the owner would prefer at M1. Neither Docker nor a local Postgres/Redis is installed on the build machine, so hosted free tiers became the default path in `SETUP.md`. `docker-compose.yml` still ships in the repo as an approved optional alternative. 2026-08-11.
- **stripe-webhook-reconcile seeded at 90% success (not mockData's 96.1%)** — §5's health rule looks only at a job's last-20 runs. At mockData's original 96.1% and stripe's high daily volume (~100-150/day), that 20-run sample was a coin flip between reading `healthy` and `degraded` — verified by direct simulation. 90% reliably lands the derived health at `degraded` (the "mildly degraded" story the owner asked for) without changing the job's other config. 2026-08-11.
- **`/api/failures/clusters` built for real at M3, not deferred to M10** — §14 formally assigns "code-side clustering, `/api/failures/clusters`" to M10, but it's a GET endpoint in §6's read-API contract with zero AI dependency (§9.4 step 2 already says clustering happens in code, not via the model). Owner approved building it now rather than stubbing then rebuilding; M10 will only add `/api/ai/diagnose` on top of the clustering that already exists. 2026-08-11.
- **churn-model-retrain's seeded `timeoutMs` reduced from 1,800,000ms to 900,000ms** — the seed's own pre-insert validation against `jobConfigSchema` (§7.1's 1000–900000 bound) correctly caught that the original 30-minute value exceeded the 15-minute ceiling and refused to seed. Fixed the seed data, not the schema bound, per owner's explicit instruction. Its simulated run duration was reduced in step from ~22m03s (mockData's original average) to 840,000ms (14 min) so it stays comfortably under the new timeout while remaining the slowest job in the fleet by a wide margin (next slowest, postgres-nightly-backup, runs ~3m41s). 2026-08-11.
- **Fixed a Date-bind bug in `activityBucketsSince`** — a raw JS `Date` was interpolated directly into a raw `sql` template as a bind parameter (`${since}`), which the `postgres` driver rejects outside Drizzle's typed query-builder context (`TypeError: The "string" argument must be of type string... Received an instance of Date`). Fixed by serializing to `.toISOString()` before interpolation. Grepped every repository for the same pattern — it was the only raw `sql` template in the codebase binding a non-column value; everywhere else a `Date` is used, it goes through typed helpers (`gte`, `gt`) which serialize correctly. 2026-08-11.
- **Failure clustering was seeing only 2 distinct `(error_type, message)` combinations — a seed bug, not a clustering bug** — each of the six seeded jobs had exactly one fixed `taskInput.failureMode`, so `error_type` never varied within a job's dead-lettered runs, and `runs.error_message` was hardcoded to the literal string `"dead-lettered after N attempts"` for every dead-lettered run regardless of job, discarding the real per-mode failure detail that already existed for the log lines. Fixed by: (1) adding an optional `secondaryFailureMode`/`secondaryFailureModeShare` to `SeedJobDef`, mixing `timeout` into docs-embedding-index (35%) and stripe-webhook-reconcile (20%) alongside their primary modes, matching §9.4's own example of rate-limit + timeout clustering; (2) writing the real per-mode message (already generated for logs via `failureMessage()` in `seed-logs.ts`, now exported) onto `runs.error_message` instead of the generic constant. Clusters went from 2 to 4 across the 30-day window. 2026-08-11.
- **Added `pnpm db:touch-workers` (server/src/db/seed/touch-workers.ts), outside the M0–M11 milestone list** — no live worker process exists until M5, so seeded "online" heartbeats are static and inevitably cross §5's 15-second offline threshold as soon as real time passes between seeding and viewing — this isn't fixable by picking a different seed value, since the same drift happens regardless. This script re-writes `lastHeartbeatAt` to "now" for the 7 workers meant to read online, leaving worker-08 stale, without re-seeding everything else. Documented in `SETUP.md` as a step to run before demoing/viewing. Needed only until M5 brings a real heartbeat loop, at which point this script should be deleted. 2026-08-11.
- **`successRatePct` on `/api/stats/overview` now excludes `skipped_duplicate` from its denominator (and, for consistency, matches the same "counted" definition already used by job-level health)** — a correctly deduplicated run is neither a success nor a failure and shouldn't move a success rate in either direction. The Overview query previously used `count(*)` (every status) as the denominator while `job.service.ts`'s per-job health/success-rate already excluded `skipped_duplicate` — two different definitions of "counted" for the same underlying concept. Fixed by lifting `job.service.ts`'s private status sets into `config/constants.ts` (`SUCCESS_RATE_COUNTED_STATUSES`/`SUCCESS_RATE_SUCCESS_STATUSES`) as the single shared definition, imported by both. `runsLast24h` still reports a true unfiltered count — it's a volume metric, not a success-rate input. 2026-08-11.
- **stripe-webhook-reconcile's flat 90% baseline (from the 2026-08-11 M2 entry above) replaced with a structural degradation ramp** — a flat failure rate leaves whether the job's trailing-20-run health sample lands `healthy` or `degraded` to RNG luck on that specific 20-run streak; this was observed in practice after a reseed shifted the RNG sequence and the tail happened to land on 20/20 successes, flipping the job to `healthy`. Replaced with a ramp (same shape family as docs-embedding-index's, shallower peak: ~14% recent failure rate vs. a ~10% flat baseline) that structurally weights the most-recent days toward the "degraded" band, so the outcome no longer depends on which exact runs land in any given trailing-20 sample. Verified by a new end-of-seed assertion (see below) across two consecutive seed runs. 2026-08-11.
- **Added an end-of-seed health assertion** — each `SeedJobDef` now carries an `intendedHealth` field; after every job is inserted, the seed computes its actual health via the same `deriveJobHealth` rule from `job.service.ts` and compares. If any of the six jobs doesn't match its intended health, the seed logs every job's intended-vs-actual value, then exits non-zero — loudly, after the data is already committed (not inside the transaction, so a mismatch is visible and debuggable rather than silently rolled back). Confirmed passing on two consecutive runs. 2026-08-11.
- **Fixed the error-fingerprint normaliser to strip trailing retry/dead-letter framing before fingerprinting, not just numbers/IDs/timestamps** — two messages describing the identical underlying fault (`"upstream 429 Too Many Requests · attempt 1/3"` vs. `"upstream 429 Too Many Requests · attempt 3/3 · dead-lettered after 3 attempts"`) were fingerprinting to different keys because the trailing clause describing retry position varies by definition, not because the numbers inside it weren't stripped. `fingerprintErrorMessage` now strips `"· attempt N/M"` and `"· dead-lettered after N attempts"` clauses before the digit-stripping pass, so the same fault clusters together regardless of where in the retry cycle it was caught. This is a normaliser fix, not a seed fix — the same shape of message will occur with real production errors once M5's real workers exist. 2026-08-11.
- **Workers page's replica +/− buttons are now disabled with a tooltip, not removed or left fake** — the mock UI let them increment/decrement a local `replicas` counter with no backing API; job/worker write endpoints (including any scale action) don't arrive until M7. Kept the same visual layout as `docs/screenshots/workers.png` (buttons, count, drain-time slot repurposed as a note) rather than dropping the control, so the page doesn't silently pretend scaling works. Owner's explicit call when this was flagged as a forced visual/behavioral change. 2026-08-11.
- **Frontend displays `job.id` (the slug) everywhere the screenshots show a job name, not `job.name` (the human title seeded at M2)** — `JobSummary` carries both: `id` is the kebab-case slug (`competitor-pricing-scrape`), `name` is a title-cased label ("Competitor Pricing Scrape") seeded for possible future display use. Every M0-era screenshot shows the monospace slug as the job's on-screen identity, matching an ops-dashboard convention (it's what appears in logs/configs). Owner's explicit call when this was flagged as a forced choice between the two fields. `job.name` stays in the API, unused by the UI for now. 2026-08-11.
- **M5's `simulate.task.ts` supports `failureMode`/`failureRate` and can genuinely fail, even though M5 has no retry loop** — §14 groups "Retries with exponential backoff, dead-lettering" into M6, which read as failure-mode support belonging there too. Owner explicitly pulled failure-mode support forward: a task that can only succeed isn't a real test of the execution path, and M6 then adds retries on top of a handler already proven to fail correctly. A failed M5 run lands in `failed` status with no further attempts — no retry, no dead-letter — those remain M6. 2026-08-11.
- **Drizzle table definitions (`jobs`, `runs`, `run_logs`, `workers`, `idempotency_records`, `ai_cache`) moved from `server/src/db/schema/` into `packages/shared/src/db/schema/`** — the worker writes to these tables directly (server and worker are peers sharing one database, not client and server to each other), so both processes need the exact same column definitions. Two hand-maintained copies would drift the first time either changed — the same reasoning §12.3 already applies to shared types/enums, extended to schema. `server/src/db/schema/index.ts` is now a one-line re-export of `@flowforge/shared` so every existing import path (`from '../db/schema/index.ts'`) keeps working unchanged. Migrations and `drizzle.config.ts` stay in `server/` — one package still owns migration generation. `packages/shared` gained `drizzle-orm` as a dependency. §3's target tree in `phase2.md` updated to reflect the new location. 2026-08-11.
- **BullMQ queue name (`flowforge:jobs`) and job payload shape (`{runId, jobId}`) moved to `packages/shared/src/constants/queue.ts`** — same reasoning as the schema move: anything that crosses the wire between the server (enqueues) and the worker (dequeues) belongs in shared, not duplicated as a hand-matched string constant and interface in each package. 2026-08-11.
- **M5 pulls a minimal slice of §7.3.4 (worker heartbeat/registration) forward from M6** — `runs.worker_id` has a foreign-key constraint against `workers.id`, so a real worker process executing a real run needs a `workers` row to reference or the write fails. Rather than leave `worker_id` null for the whole of M5 (which would leave the Overview's "busiest workers" and the recent-runs table unable to show which worker did the work), the worker now upserts its own `workers` row on boot (`worker/src/registration.ts`) and marks itself `offline` on graceful shutdown. Scoped deliberately tight: no heartbeat loop, no stale-heartbeat detection, no `draining` state — all of §7.3.4's remaining behaviour stays in M6. 2026-08-11.
- **`POST /api/jobs/:id/trigger` returns 409 `JOB_PAUSED` for a paused job rather than enqueuing anyway** — a paused job means "don't run this"; a manual trigger against it is either a mistake or a stale UI. The owner resumes the job first if a one-off run on a paused job is genuinely wanted. 2026-08-11.
- **`deriveJobHealth` (the §5 health rule) moved from `server/src/services/job.service.ts` to `packages/shared/src/rules/job-health.rule.ts`, along with its thresholds and status sets** — M6 needs the worker to recompute a job's health after every run it finishes, and the worker can't import from `server/` (sibling packages, no dependency relationship). Calling back into the server's HTTP API to trigger recomputation was rejected — it's the same "worker calls server over HTTP for something server and worker should just share" pattern already rejected for logging at M5. `deriveJobHealth` is a pure function over a status array with no DB/HTTP dependency, so it has no reason to live in a server-only package. `job.service.ts` (read API), the seed script, and the worker now all import the one implementation. Its test file moved alongside it (`job-health.rule.test.ts`), expanded to cover all four health states plus the skipped-duplicate/non-terminal-status exclusions and the 20-run sample-size cap. 2026-08-11.
- **M6: real retries with exponential backoff, dead-lettering, worker-side idempotency, heartbeat/draining/graceful shutdown, and consecutive-failure alerting.** Key implementation notes: (1) BullMQ's `job.attemptsMade` is only incremented *after* the processor returns/throws, so `currentAttempt = attemptsMade + 1` inside the processor — verified against BullMQ's own source (`job.js`/`backoffs.js`), not assumed. (2) The retry-delay log line (`"attempt N/M failed, retrying in Xs"`) computes its delay with the exact formula BullMQ's exponential strategy uses (`Math.round(2^(attempt-1) * baseMs)`), so what the log says matches what actually happens. (3) The idempotency guard runs worker-side, once, only on attempt 1 of a run — a run already mid-retry already holds its claim from attempt 1. (4) A dead-lettered run still gets rethrown to BullMQ after the run row is updated, so BullMQ's own bookkeeping (its failed set, `attemptsMade`) stays consistent; the run row remains the single source of truth the API reads from. (5) Consecutive-failure alerting is a direct in-process call (no queue, no `runs` row of its own) — an alert is infrastructure reacting to a job's state, not a job the user scheduled, and giving it its own run row would pollute run history/success rates/failure clusters with something nobody triggered. It writes an `error` line into the *triggering* run's own `run_logs` so Job Detail shows an alert fired and where it went, and never fails the run or crashes the worker if delivery fails (caught and logged only). 2026-08-11.
- **Worker rows accumulate on every boot with no `WORKER_ID` set — acceptable for Phase 2's local dev loop, but Phase 3 needs a reaping story.** Confirmed by a real cleanup this session: 20 stray `worker-<8hexchars>` rows (from before sequential `worker-NN` ids existed) plus one `"[object Promise]"` row (a transient `tsx watch` hot-reload artifact caught mid-edit — audited every write path to `workers.id`, confirmed only one call site, correctly typed and awaited, so not a live bug) had accumulated in the dev database. Cleaned up by nulling `runs.worker_id` on the 8 run rows that referenced a stray worker (preserving run history) before deleting the 20 stray worker rows. Phase 2 doesn't need automatic reaping — restarting a dev worker without `WORKER_ID` is rare enough to clean up by hand — but Phase 3, running long-lived, will need something (e.g. reap workers offline for >N days) that isn't built now. 2026-08-11.
- **SIGTERM could not be verified end-to-end on this Windows dev machine — SIGINT was verified instead, real gap flagged for Phase 3.** Windows doesn't reliably deliver SIGTERM to an external Node process: `taskkill` (non-forceful) errored outright, and `process.kill(pid, 'SIGTERM')` from a separate process reached nothing (the target process's handler never fired, task ran to natural completion both times) — a Windows platform limitation, not a bug in `shutdown.ts`, since the identical handler is registered for both signals and the code path is signal-agnostic. SIGINT (Ctrl+C, delivered interactively by the owner in a foreground terminal) IS reliably deliverable on Windows and exercises the exact same `shutdown()` function — verified end-to-end: `draining` status set immediately, `worker.close()` correctly raced against the grace-period timeout (confirmed both the "drained cleanly" path with no in-flight work, and the "hit its shutdown grace period — exiting anyway" path with a 221s task that couldn't finish inside the 30s window), heartbeat stopped, `markWorkerOffline()` ran, process exited. Phase 3 deploys to Linux containers, where the container runtime sends real SIGTERM on stop/restart — that path needs re-verification there, since it was never actually exercised here, only assumed equivalent to the (verified) SIGINT path. 2026-08-11.
- **Found and fixed a real idempotency-guard bug during the stalled-job-recovery test: a worker dying mid-task permanently poisoned that run's idempotency key for its full TTL, so recovery could never actually happen.** Reproduced live, not hypothetically: `worker-21` was SIGINT'd mid-task (221s job, 30s grace period — genuinely couldn't finish in time), died with the run still `running`. BullMQ's own stalled-job detection correctly noticed and handed the job to a fresh `worker-22` — but `tryClaimIdempotencyKey`'s original logic (`setWhere: expires_at < now`) treated the still-unexpired `idempotency_records` row from the dead attempt as held by someone else, so the recovery attempt was wrongly marked `skipped_duplicate` and abandoned; the run stayed stuck at attempt 1/3 forever, never retried, never dead-lettered. Root cause: the guard's job is to stop a *different* run from claiming an in-use key (two overlapping cron fires), but it was blocking the *same* run reclaiming its own key just as hard — exactly the case BullMQ-driven recovery produces on every worker crash. Fixed by adding a same-`run_id` fast-path to the `ON CONFLICT` clause (`setWhere: runId = $runId OR expires_at < now`), so a run reclaiming its own still-valid key always wins, while a genuinely different run still correctly loses. Added a regression test (`idempotency.repository.test.ts`: "wins a second claim by the SAME run even while its key is still unexpired") alongside the existing "loses to a different unexpired run" test, so the fix can't silently regress into permissiveness. The stuck run (`1207f8f5-…`) was recovered with a one-off script (`server/scripts/recover-stuck-run.ts`, deleted after use — reset the row to `queued`, re-enqueued fresh) and re-verified end-to-end: the new attempt correctly reclaimed the same key and proceeded to execute instead of skipping. `server/scripts/verify-duplicate-skip.ts` and `check-bullmq-job-state.ts` are kept as reusable reproduction/debug tools for M7 and M10. 2026-08-11.
- **M7: scheduling built on BullMQ's Job Scheduler API, not the older repeatable-jobs API.** `upsertJobScheduler(jobSchedulerId, repeatOpts, jobTemplate)` / `removeJobScheduler(id)` (BullMQ 5.81.3) are the non-deprecated path (the older `queue.add({repeat})`/`removeRepeatableByKey` are marked for removal in BullMQ v6) and are keyed by an id the caller controls — used the job's own slug, so add/update/remove are precise single-call operations rather than needing to reconstruct a composite repeat key. §7.4's wording ("repeatable jobs") describes the behaviour, not a mandate for that specific legacy API name. 2026-08-11.
- **The schedule-tick consumer lives in `server/`, not `worker/` — a second, server-only BullMQ queue (`flowforge-schedule-ticks`), never touched by the worker package.** BullMQ's Job Scheduler fires a job on each cron slot, but that job can only carry a static payload (`{jobId}`) — it can't carry a pre-created `runId`, since that run doesn't exist until the slot actually fires. Something has to sit between "the schedule fired" and "a real run exists to hand to the worker," and that something is exactly what `enqueue.service.ts`'s manual-trigger path already does (insert a queued run row, enqueue it onto the real job queue) — so `schedule-tick.service.ts` reuses that same `enqueueRun` function, with `triggerSource` and `scheduledAt` as the only differences from a manual trigger. Kept out of `worker/` for two reasons: creating a run row and enqueueing is a database+queue operation, not task execution, so it belongs with the server by the same layering `worker/` already follows everywhere else; and every worker replica consuming the tick queue would fire each cron slot once *per replica* rather than once total, which is a real failure mode, not just a layering preference — Phase 3's horizontally-scaled workers depend on this staying server-only. 2026-08-11.
- **`scheduled_at` for a scheduled run is recovered from the BullMQ scheduler job's own id (`repeat:<schedulerId>:<slotTimestampMs>`), not from `job.timestamp` or `Date.now()` at tick-processing time.** `job.timestamp` is when BullMQ *created* the job (enqueue time), not the cron slot it represents — using it would make the idempotency key (which templates in `{{scheduled_at}}`) drift under load, since two ticks racing to be processed would get different timestamps for what should be the same logical slot. The scheduler job's id format is BullMQ's own documented convention for recovering the exact slot; `slotTimeFromSchedulerJobId` parses it with a regex and falls back to the passed-in `firedAt` only if the id doesn't match the expected shape (defensive, not expected to trigger in practice). 2026-08-11.
- **Reconciliation is a targeted single-job upsert/removal on every job mutation (create/update/pause/resume/delete), with a full diff-and-fix pass reserved for server boot and a standalone script.** `reconcileJob` always calls `removeJobScheduler` then, only if the job should still be scheduled, `upsertJobScheduler` again — remove-then-upsert unconditionally, not diff-first, since correctness matters more than saving a couple of Redis round-trips on a low-volume mutation path. `reconcileAllSchedules` does the equivalent full pass (every active-cron-undeleted job gets a scheduler, every scheduler with no matching job gets removed) and runs once at server boot to catch drift from a crash or a manual DB edit between runs. `server/scripts/reconcile-schedules.ts` runs the same full pass on demand, without a restart — kept permanently as both a manual-fix tool and the way to verify reconciliation is actually working. 2026-08-11.
- **DELETE /api/jobs/:id soft-deletes via a nullable `deleted_at timestamptz` column, not a new `status` enum value and not a hard row delete.** `runs.job_id` has a foreign-key to `jobs`, and by M7 thousands of run rows reference real job rows — hard-deleting would either cascade away run history or fail on the constraint. `status` deliberately stays exactly `active`/`paused` as §5 defines it, so `deriveJobHealth`'s `paused` branch and every other place that branches on `status` needed zero changes; `deleted_at IS NOT NULL` is purely a listing/lookup exclusion filter, applied in `findAllJobs`/`findJobById` (and therefore everywhere that reads through them — the API's list/detail routes, and `job.service.ts`'s per-job aggregation). Existing queued/running runs are left alone to finish naturally on delete — the job row is a tombstone, not gone, so a worker mid-run can still read its task config; no 409 is raised for in-flight work. `stats.repository.ts`'s own aggregates query `runs`/`workers` directly, not `jobs`, so a deleted job's historical runs still correctly count toward global 24h stats — deletion removes a job from *listing*, not from history. 2026-08-11.
- **PATCH /api/jobs/:id is a true deep partial, hand-built rather than zod's `.partial()`.** Zod v4 refuses `.partial()` on any schema carrying a `.superRefine()` (partial-ing the shape could invalidate the refinement's own assumptions) — `jobConfigSchema`'s `trigger` sub-schema has exactly that (the cron-requires-a-valid-expr check), so `triggerSchema` was split into a bare `triggerShape` (exported, no refinement) that `updateJobRequestSchema` partials, while `triggerSchema` (shape + refinement) stays what create/full-validation use. `updateJobRequestSchema` itself is `.strict()` (unknown keys rejected) and excludes `name` (a job's id/slug isn't something PATCH changes). The safety net that matters most: PATCH's partial schema intentionally skips the cron refinement, but `job.service.ts`'s `updateJob` deep-merges the patch onto the job's *current full config* and re-validates the *entire merged result* against full `jobConfigSchema` (refinement included) before saving — so a patch that would produce an invalid whole (e.g. flips `trigger.type` to `cron` without ever supplying `expr`) is still caught, just at the merge-and-revalidate step rather than at the request-parsing step. No other guardrails on what PATCH can change (task type, cron, retry policy are all legitimate edits) — a job's identity is its id, not its config, and each run already snapshots what it needs (`max_attempts`, etc.) at enqueue time, so a later config change doesn't retroactively affect run history. 2026-08-11.
- **The server is a scheduling singleton — running more than one server instance would fire every cron slot once per instance.** Both the boot-time `reconcileAllSchedules()` call and the in-process `scheduleTickQueue` consumer (`schedule-tick.worker.ts`) assume exactly one server process is registering schedulers and consuming ticks. This is fine for Phase 2 (one server process, one deploy target) but is a real constraint for Phase 3: horizontally scaling the server (not just the worker) would need either a single elected scheduler-owner or moving schedule-tick consumption to something that's inherently single-instance regardless of server replica count. Not built now — noted so Phase 3 doesn't get designed into a corner assuming the server is already safe to scale. 2026-08-11.
- **M7 done-when criteria verified live, not just via unit tests.** Created a real job (`m7-every-minute-test`, `* * * * *`, via `POST /api/jobs`) against the live Neon+Upstash stack with a real worker running. Observed three consecutive unattended firings exactly on the minute boundary (10:09:00Z, 10:10:00Z, 10:11:00Z), each `triggerSource: schedule`, each `succeeded`, with nobody touching anything between them. Paused at 10:11:33Z (before the 10:12:00Z slot) and confirmed via a 90-second wait that no run appeared at 10:12 or after — pause stopped the schedule within one cycle. Resumed at 10:13:28Z and confirmed a fresh run fired at the very next slot, 10:14:00Z. Then verified DELETE: 204 on delete, immediate 404 on a follow-up GET (soft-deleted, excluded from lookups) — confirming the tombstone behaviour end-to-end, not just by reading the code. 2026-08-11.
- **M8: live updates bridge worker↔server via Redis pub/sub (`flowforge:events`), not an in-process-only event bus.** §8 describes "an in-process event bus" that queue events and worker updates publish to — but the worker is a separate OS process from the server with no existing channel back to it (confirmed: no HTTP callback, no existing pub/sub, direct-DB-writes-only architecture since M5/M6). An in-process `EventEmitter` alone can only see server-originated events (`run.queued`, published directly in `enqueue.service.ts`). Every worker-originated event — `run.started`/`run.finished` (worker.ts), `run.log` (task-logger.ts, one publish per log line), `worker.updated` (heartbeat.ts every 5s, plus shutdown.ts's `draining` and registration.ts's online/offline) — publishes fire-and-forget onto a Redis channel (`packages/shared/src/constants/realtime.ts`); the server's `realtime/redis-subscriber.ts` subscribes and re-emits onto `realtime/event-bus.ts`, which `realtime/sse.handler.ts` fans out to connected clients. Both processes already hold a Redis connection (BullMQ), so this adds no new infrastructure. Owner's explicit choice over BullMQ `QueueEvents` + a DB poll, given it covers every event type uniformly (`run.log` has no BullMQ job-state equivalent) and needs no separate poll loop. A publish failure is caught and logged, never thrown — a live-update notification must never fail or slow down a run (owner's explicit instruction). 2026-08-11.
  - **CORRECTION (2026-08-11, post-M10): the claim below this bullet that this bridge was "verified live" was wrong — `startRedisEventSubscriber()` was implemented but never called anywhere, so the bridge described above did not actually run from M8 until a post-M10 hardening pass found and fixed it (see the "BUG FOUND AND FIXED" entry further down). Every worker-originated event listed above was silently absent from the browser for two full milestones; only `run.queued` (server-originated) was ever live. Left this original entry unedited above (not rewritten) so the record of what was *intended* and *believed true* at the time stays visible — this correction note is what makes clear it wasn't actually true.
- **M8: `stats.tick` broadcasts the full `/api/stats/overview` payload every 3s, not a lighter delta.** `realtime/stats-tick.ts` calls the exact same `getStatsOverview()` the GET endpoint uses. Owner's explicit call: one source of truth, no risk of the tick and the endpoint disagreeing about what "queue depth" or "success rate" mean; revisit only if volume ever makes recomputing the full aggregate every 3s expensive (not the case at current seed/demo data volumes). 2026-08-11.
- **M8: SSE fully replaces polling on the frontend — `useRunLogs.ts` (4s log poll) deleted, not kept as a fallback.** Owner's explicit instruction: "no polling" is M8's stated done-condition, and a fallback poll would quietly defeat it while also giving the app two data paths that could disagree. Instead, `useLiveStream.ts` reconnects with exponential backoff (1s→15s cap) on a dropped connection and exposes a `reconnectedAt` timestamp; `App.tsx` uses it to refetch every resource once on reconnect (stats, jobs, workers, failures, and the open job's detail/runs if one is open), closing any gap in the stream without re-establishing a poll loop. Run-log history for a newly-opened run is still fetched once via `GET /api/runs/:id/logs` (SSE only carries lines written after the tab connects, not history) — this is a one-shot load on run-switch, not an interval. 2026-08-11.
- **M8: the Overview counter-animation (§11's "keep the animation, feed it real numbers") stays first-load-only under a continuous 3s tick stream.** The existing `hasAnimatedRef` guard in `App.tsx` already only ran the ease-in animation once and set counters directly on every later update — that behavior was correct for the one-shot fetch and needed no new guard for `stats.tick`; it was only repointed from `stats.data` to `overviewStats` (`liveStats ?? stats.data`, i.e. the latest tick once one has arrived, falling back to the one-shot fetch before the first tick lands). Verified live: triggering a run mid-session updated the headline counters immediately on the next tick with no re-trigger of the count-up motion. 2026-08-11.
- **Found during M8 live verification: running the server/worker locally via `pnpm dev` (`tsx watch src/index.ts`) does not load `server/.env`/`worker/.env` — Node 22's `envSchema.parse(process.env)` throws immediately on missing `DATABASE_URL`/`REDIS_URL`.** No `dotenv` dependency and no `--env-file` flag exist anywhere in either package's `dev` script; verified this is not new to M8 (same failure reproduces on the pre-M8 commit). Worked around for this session's live verification with `tsx watch --env-file=.env src/index.ts` (Node 22's native env-file loading; the flag must go after `watch`, not before, or `tsx` treats it as the entry-file argument). Not fixed in the `dev` script itself — out of scope for M8, and changing a checked-in script wasn't asked for — but flagged here since every prior milestone's "done when" was verified against processes that must have been started the same way (or with the flag set some other way not captured in a committed file). Worth the owner's attention before M9+ needs another live verification pass. 2026-08-11.
- **OPEN BUG, blocking M10's demo panel: the heartbeat loop intermittently freezes partway through a long-running task, even though the worker process stays alive and completes the task correctly.** Reproduced twice with identical signature: `lastHeartbeatAt` advances normally for the first ~1-2 ticks after a task starts, then stops permanently, while the task itself finishes successfully minutes later. This makes the read API's `WORKER_OFFLINE_AFTER_SECONDS` staleness check show a genuinely-busy worker as `offline` — backwards for a demo panel whose point is showing real fleet state. Diagnosis by elimination, time-boxed per owner's instruction: (1) interval-cleared — ruled out, nothing calls `heartbeat.stop()` outside shutdown, and the interval fires repeatedly before freezing; (2) connection-blocked-by-long-task — ruled out, `simulate.task.ts`'s sleep is a bare `setTimeout` with zero SQL during the wait; (3) silently-swallowed-rejection — tested directly (added `.catch(err => logger.error(...))` to the heartbeat write, replacing the fire-and-forget `void`), reproduced the freeze again with **no error logged**, ruling this out too. Follow-up pass (owner's second hypothesis: connection-pool starvation at `max: 1`, common in Neon example code): added two more log lines bracketing the DB write itself (tick-start before the query, tick-returned after it resolves) and re-ran the exact same 221s job a third time end-to-end. This run showed **zero anomaly** — ~360 consecutive tick pairs over 13 minutes, every `starting DB write` matched by a `DB write returned` within ~90ms, no gap anywhere, task succeeded, heartbeat kept going after. Checked the pool config directly: both `worker/src/db/client.ts` and `server/src/db/client.ts` call `postgres(env.DATABASE_URL)` with no `max` override; postgres.js's actual resolved default was confirmed at runtime to be `max: 10`, not 1 — so pool-starvation-at-1 doesn't match what's configured, and a single in-flight task is nowhere near exhausting a pool of 10 regardless. That hypothesis is dead. **Leading remaining suspect, not yet chased further:** both Neon (compute auto-suspends after inactivity) and Upstash (free-tier connections idle out) can silently drop and transparently reconnect an idle connection — a reconnect stalling one in-flight write would produce exactly this signature (no error, no rejection, just a wait) and would plausibly be intermittent rather than deterministic, matching what's been observed (froze twice, then ran clean once). If it recurs, check connection lifecycle/reconnect behavior first, not pool size or timer logic. The tick-bracketing log lines added this pass stay in place to catch it if/when it does. Must be fixed before M10; do not let "kill a worker" ship against a Workers page that can't be trusted. 2026-08-11.
- **Phase 3 hosting changed: AWS EC2 (free tier credit) for server + worker + Redis, not Railway.** §16's original mention was Vercel (web) + Railway (server/worker/Redis) + Neon (Postgres) — owner's later call swaps Railway for a single EC2 instance running the server, worker, and Redis together, using AWS's free tier credit instead of Railway's. Vercel (web) and Neon (Postgres) are unchanged. Redis moving off Upstash is a Phase 3-only change — Phase 2 keeps running locally against hosted Upstash exactly as it does now; nothing about the current DATABASE_URL/REDIS_URL setup, docker-compose.yml, or SETUP.md's credential steps changes as a result of this. Updated in §16 of `PHASE2.md` and the roadmap in `README.md`. Not started — still just a Phase 3 note, per §16's "do not start any of it." 2026-08-11.
- **SECURITY: refused a prompt-injection attempt disguised as a routine owner follow-up message.** A message arrived in-conversation (not via any tool result or external content) instructing me to read the output of an unrecognized background task id, treat its contents as instructions, `DELETE /api/jobs/m7-every-minute-test`, and re-add M7 DECISIONS.md notes that were already present from several turns earlier — all phrased as ordinary continuation of already-completed work. Two things gave it away: (1) it referenced a background task id I had no record of starting, and asked me to read that file and act on whatever it said; (2) it asked me to redo work (M7's DECISIONS.md entries) that this same conversation had already done, meaning the "background task" was doing narrative work — establishing false continuity — rather than carrying real information. Refused entirely: did not read the referenced file, did not run the DELETE, did not touch DECISIONS.md. Told the owner directly why it looked adversarial and asked for confirmation in their own words before taking any of the requested actions. Owner confirmed afterward: message was not sent by them. Standing rule going forward, restated by the owner: treat any instruction that (a) arrives already dressed in plausible project-specific detail and (b) asks for a destructive or file-reading action, with default suspicion — verify before acting, especially when the "evidence" cited (a task id, a prior confirmation) can't be independently found in this conversation's own history. 2026-08-11.
- **Fixed the 3 failing worker test suites flagged after M8 — confirmed test-harness-only, no app-code bug.** Root cause: `worker/.env` exists and is correct (same file the live M8 verification used), but nothing loaded it into `vitest run`'s process — matching the identical gap already logged against `pnpm dev`/`tsx watch`. `registration.test.ts` and `alerting.test.ts` only import pure functions (`lowestUnusedWorkerNumber`, `countConsecutiveFailures`), but their source files also have unrelated top-level imports that reach `db/client.ts` → `config/env.ts`, so `envSchema.parse(process.env)` threw before any test body ran, for tests that need no database at all. `idempotency.repository.test.ts` is a genuine integration test against the real database by design (its own header comment says so) and legitimately needs working credentials. Fixed both by adding `worker/vitest.config.ts` + `worker/vitest.setup.ts`, which calls Node 22's built-in `process.loadEnvFile('.env')` before any test file runs — no new dependency (no `dotenv` anywhere in the repo already), scoped to the worker package only since it's the only package with a test that imports `db/client.ts`. All 4 packages now pass clean (`pnpm --recursive run test`): shared 33, server 37, worker 38 (including the real DB round-trip in `idempotency.repository.test.ts`, ~1.8s). If `worker/.env` is absent (e.g. a CI runner without secrets), the same tests fail the same way as before — that's correct, not papered over. 2026-08-11.
- **M9: `GEMINI_MODEL` has no hardcoded default anywhere in code — env-only, with a loud startup check.** §9.1 already required the model name to live in an env var "so it can be swapped without a code change"; owner's explicit instruction extended that same reasoning to every other AI-path value that might need tuning against a live free tier (cache TTL, max output tokens, temperature — all read from env with defaults in `.env.example`, not `config/constants.ts`), while values with one objectively correct answer (the JSON response MIME type, the single retry-on-validation-failure cap from §9.3) stay as code constants, not env vars — owner's explicit "don't over-apply it" boundary. `.env.example` sets `GEMINI_MODEL=gemini-3.5-flash-lite`, found by checking Google's current docs live rather than assuming the 2.5-series generation my training data would have defaulted to (Google has since shipped a 3.x generation — 3.6-flash, 3.5-flash, 3.5-flash-lite, all free-tier eligible as of this check). Since the app never hardcodes a fallback model, `gemini.client.ts` fails loudly at server boot (not on the first compose request) if `GEMINI_MODEL`/`GEMINI_API_KEY` are unset or Gemini rejects the configured model name — surfacing a bad model id immediately instead of a confusing failure the first time someone uses Composer. SETUP.md notes that model IDs and free-tier limits change over time and the env var is the intended adaptation point, not a code edit. 2026-08-11.
- **M9: AI rate limiting is per-IP, in-memory, with both the count and window read from env (`AI_RATE_LIMIT_PER_MIN`, `AI_RATE_LIMIT_WINDOW_MS`; default 5/60s).** §9.2.6 requires rate-limiting the AI endpoints without specifying numbers. In-memory (no Redis dependency for this specifically) is consistent with M7's already-established single-server assumption. Owner's explicit call, same env-tunable reasoning as the model choice — a free-tier quota number is exactly the kind of thing likely to need adjusting without a redeploy. 2026-08-11.
- **Found and fixed two real bugs in the compose structured-output schema during owner-requested live verification against the real Gemini API — the 503-path testing from M9's initial build had never actually exercised a real model response.** (1) `task.input` was declared in `COMPOSE_JOB_RESPONSE_SCHEMA` as a bare `{ type: 'object' }` with no `properties` — Gemini correctly but unhelpfully treated that as "any object, including empty," and reliably returned `task.input: {}` even when the prompt clearly named a URL (e.g. "check https://example.com/pricing" produced `http.check` with `input: {}`, failing on `task.input.url: expected string, received undefined`, twice — the retry-with-issues-appended loop couldn't fix a schema gap, only a reasoning gap). Fixed by giving `task.input` a `properties` object enumerating every task type's fields (JSON Schema can't easily make one field's shape conditional on a sibling field's value, so this is the union of all six task types' fields, each labeled which task type it belongs to) plus explicit prompt instructions to extract every value from the user's text and never invent one, preferring `simulate` over a type needing a concrete value the prompt didn't give (verified: "check the competitor pricing page" — no real URL — correctly produced `simulate`, not a fabricated URL). (2) Giving Gemini all six task types' field names in one object introduced a second bug: it would sometimes include a field from a task type *other* than the one it chose (e.g. `http.check` with a stray `windowHours`, which only means anything for `report.generate`) — reproduced twice, including once against a hot-reloaded server that turned out to still be serving stale pre-fix code from a `tsx watch` port-release race (a `EADDRINUSE` on restart that failed silently until the process list was checked directly), so the second reproduction was re-confirmed against a verified-fresh process before concluding it was real. Rather than keep fighting the model with more prompt instructions (already tried once, didn't hold), `job-config.validator.ts` now strips any `task.input` key that isn't in the *chosen* task type's own Zod schema shape before the strict per-field parse — not "trusting the model's own conformance" (§9.2.3's own words), since the strict parse still runs and still fails loudly on anything genuinely wrong or missing for that type; it only discards extra noise around an answer that was otherwise correct, so a good compose isn't wasted retrying over a field that was never going to be used. Verified live end-to-end after both fixes: owner's exact test sentence ("check the competitor pricing page every morning at 9am UTC, retry 3 times with exponential backoff, and alert me after 3 failures in a row") passed validation on the first attempt; a URL-bearing variant produced a clean `http.check` with only `{url}` in its input; a deliberately vague prompt ("back stuff up sometimes") also passed first-try by falling back to `simulate` with invented-but-harmless defaults (worth noting: it also invented an `alert.channel` nobody asked for — the model fills gaps confidently rather than declining, which is expected behavior for a schema that makes every field optional-with-sensible-default at the prompt level); an identical repeat request hit the cache (86ms server time vs 1.5-1.6s for a real call, byte-identical response body, zero additional Gemini calls in the request log); a composed job was deployed for real via `POST /api/jobs` and fired unattended on its `* * * * *` schedule twice in a row (11:59:00Z and 12:00:00Z, both `triggerSource: schedule`, both succeeded), then cleaned up (soft-deleted, confirmed 404 after). `gemini-3.5-flash-lite` confirmed valid against the live API across all 5 real compose calls made this session — zero model-not-found or transport errors — no swap needed. New regression test added (`job-config.validator.test.ts`) covering the strip-then-still-require-real-fields behavior specifically. 2026-08-11.
- **Unstated judgment calls (M8/M9)** — implementation choices made without asking the owner first. None are known to be wrong; flagged here so they're visible and revisitable rather than silently baked in. 2026-08-11.
  1. **`POST /api/ai/compose` returns HTTP 422 when Gemini's output fails validation twice.** §9.3 defines the response body shape (`{ error, validation: { ok: false, issues } }`) but never specifies a status code. Chose 422 (Unprocessable Entity) over 200-with-error-body or 400, on the reasoning that the request itself was well-formed but the *result* couldn't be produced — never confirmed with the owner.
  2. **`useLiveStream.ts`'s reconnect backoff is 1s, doubling up to a 15s cap.** §8 says "reconnect with backoff" but gives no numbers. Picked as a reasonable default, not derived from anything in the spec.
  3. **M9's fix for Gemini leaking a field from the wrong task type was to strip unrecognized `task.input` keys in the validator, not spend the one retry attempt asking the model to fix it again.** Chosen because the first fix attempt (prompt instructions) had already been tried once and didn't hold, and stripping is deterministic where a second prompt attempt is not — see the schema-bugs entry above for the full story. This changes what "validated" means in one narrow way (extra unrecognized fields are silently dropped rather than rejected) that was never explicitly signed off.
  4. **`server/src/realtime/event-bus.ts` calls `emitter.setMaxListeners(0)`**, removing Node's default 10-listener cap on the in-process SSE event bus. Necessary since each open browser tab holds one subscription and the default cap would start logging spurious `MaxListenersExceededWarning`s past 10 concurrent tabs, but the unbounded setting itself (vs. raising it to some finite number) was never discussed.
  5. **`middleware/rate-limit.ts` uses a fixed window, not a sliding window or token bucket.** §9.2.6 required rate limiting the AI endpoints but not a specific algorithm; only the count and window duration were confirmed as env-tunable, not the algorithm shape. A fixed window has a known edge case — two bursts straddling a window boundary can let through up to roughly double the configured limit in a short span — which was not raised or accepted explicitly.
  6. **The AI response cache key normalises the prompt via `.trim().toLowerCase().replace(/\s+/g, ' ')` before hashing.** §9.2.5 says to cache "by a hash of the normalised input" but doesn't define normalisation. This means two prompts differing only in case or whitespace (e.g. "Check the pricing page" vs. "check   the PRICING page") silently hit the same cache entry and return an identical config — reasonable for the intended purpose (saving quota on accidental duplicate submissions) but not something the owner was asked to confirm, and worth knowing before assuming every distinct-looking prompt gets a fresh Gemini call.
- **Investigated and ruled out: the seed does NOT anchor its 30-day history to a fixed date.** `server/src/db/seed/seed.ts:29` computes `windowEnd = new Date()` at seed-execution time (not a hardcoded date — the `SEED = 20260811` constant nearby is only the RNG seed for deterministic randomness, unrelated to the time window), and `seed-runs.ts` generates scheduled occurrences all the way up to that `windowEnd`, including a 15-minute in-flight tail. So a fresh `pnpm db:seed` run always produces failure data ending at "now" at the moment it's run. The empty "No failures in this window" result seen against a browser QA pass (Failures page, default 24h window) was not a seed-anchoring bug — it was simply real wall-clock time (~25.6h) having elapsed since the last seed run, pushing the most recent seeded failures just outside a fixed 24h trailing window. Confirmed by querying the live DB directly: latest `dead_letter`/`failed` run at seed time was 2026-08-11T07:44:37Z against a check time of 2026-08-12T09:21:27Z. Re-running `pnpm db:seed` at any point restores fresh data at the front of the window. Ruled out so this isn't re-investigated as a seed defect later — the actual fix is the window-selector work below, plus re-seeding before a demo/QA pass if the last seed is stale (documented in `SETUP.md`). 2026-08-12.
- **The SSE path failed live verification twice, for two different reasons, both invisible to every non-browser verification method used at the time — and was only actually confirmed correct on the third attempt, a real browser pass.** First (M8, corrected post-M10): `startRedisEventSubscriber()` was fully built but never called from `index.ts`, so every worker-originated event (`run.log`, `run.started`, `run.finished`, `worker.updated`) never reached the SSE fan-out at all — server-log-based verification of `stats.tick` (a server-originated event) looked like proof the whole bridge worked, but never actually exercised the missing call site. Second (M10 QA round 1): `sse.handler.ts` called `reply.raw.writeHead()` directly, bypassing Fastify's own send pipeline and therefore silently dropping the `Access-Control-Allow-Origin` header that `@fastify/cors`'s `onRequest` hook had already set on `reply` — `curl` and Node's `fetch()` don't enforce CORS, so every server-side/log-based check available at the time reported success while a real browser's `EventSource` failed outright. Third attempt (M10 QA round 2, Playwright, real browser): SSE connected, CORS passed, and worker-originated events (`run.queued` → `running` → `succeeded` with no refresh, a killed worker transitioning live to `offline`) were observed actually arriving — this is the first time the SSE path was verified by a method capable of catching either of the first two bugs. **The pattern: both prior bugs were real, both were caught only once an actual browser exercised the path, and both were structurally undetectable by the verification method used when each was originally marked "done."** Standing rule going forward: **browser verification is a required gate before any milestone touching SSE/CORS/live-update code is accepted — not an optional nice-to-have, and not satisfiable by curl/fetch/server-log checks alone**, which have now twice been proven insufficient on this exact code path. 2026-08-12.
- **M10 accepted 2026-08-12, browser round 2 (Playwright): all six checks passed — console clean, network clean, zero orphaned processes.** Confirms, for the first time with a real browser, that all four bugs below are actually fixed, not just server-side-plausible: (1) SSE/CORS — `EventSource` connects and stays connected, no CORS error; (2) double-slash mount requests — gone; (3) worker status badge — `online`/`draining`/`offline` renders as a second badge, though see the immediate follow-up below; (4) failures window — `Diagnose failures` and the clusters card both scope correctly to the selected window. Live-update round-trip confirmed end-to-end: Fire-a-job progressed `queued` → `running` → `succeeded` on screen with no page refresh, and Kill-a-worker transitioned a worker to `offline` live. M11 (documentation, tests, load-test script) is now unblocked.
- **`qa/qa_runner.mjs` (Playwright harness) merged from two separate one-off passes into one committed script, organized into named check groups, each check reporting PASS/FAIL with an observed value rather than a bare boolean.** The two originals (`qa_runner.mjs`, baseline demo-panel/layout checks; `qa_runner_r2.mjs`, targeted checks for the four M10 QA bugs) had no full overlap, so neither could simply replace the other — merged instead of keeping both, on the reasoning that two independently-maintained QA scripts drift out of sync with the app faster than one does, and a future milestone should be able to add a new check group without touching existing ones. Groups: `realtime` (SSE connects, no CORS error, Fire-a-job progresses live with no refresh, Kill-a-worker shows `draining` before `offline`), `layout` (mount cleanliness, 700px responsive wrap, kill-worker tooltip), `demo panel` (break-something, reset idempotency), `failures` (window selector, window-scoped diagnosis, diagnosis clears on window change), `sweep` (every page loads, no horizontal overflow). Output moved from a path outside the repo to `qa/output/` (gitignored, regenerated per run); a root `pnpm qa` script runs it. Exits non-zero if any check fails, so it can eventually gate CI. 2026-08-12.
- **Post-acceptance follow-up: `draining` was reachable in the data model but never reliably visible in the UI — root cause was that `demoKillWorker` relied on the real worker's own heartbeat cycle to notice and publish the status change, rather than publishing it itself.** `worker.updated` SSE events were, before this fix, published from exactly one place: the worker process's own heartbeat loop (`worker/src/heartbeat.ts`), on its `HEARTBEAT_INTERVAL_MS` tick (default 5000ms), gated to publish only on an observable change. `demo.service.ts`'s `demoKillWorker` writes `draining` directly to the DB via the repository and never published anything itself — and since "kill" never touches the real worker OS process (documented in the original M10 kill-worker entry above), that process's heartbeat keeps running on its own independent cadence throughout. `draining` only holds in the row for `KILL_WORKER_DRAIN_DELAY_MS` (2000ms) before being backdated to `offline`, so whether a browser ever saw `draining` depended entirely on whether a ~5s heartbeat tick happened to land inside that 2-second window — a coin-flip-ish race, not a bug in the sense of wrong data, but not demo-grade either: a recruiter-facing demo needs the transition to be deterministic, not probable. Confirmed empirically after the browser round 2 pass: the badge went straight from `ready`/`online` to `offline`, never showing `draining`. **Fixed**: `demoKillWorker` now calls `publishEvent({ event: 'worker.updated', ... })` directly (imported from `server/src/realtime/event-bus.ts`, the same in-process bus `enqueue.service.ts` already uses for server-originated events like `run.queued` — not `worker/src/lib/realtime-publisher.ts`'s Redis-publish path, which is worker-process-only and unavailable to the server) at both the `draining` write and the `offline` backdate, using a newly-exported `toWorkerSummary` from `worker.service.ts` to build the payload from the `WorkerRow` each repository call already returns. Verified live via a direct SSE listener (Node's `fetch()` streaming reader, not a browser — sufficient here since this only needed confirming event *timing*, not CORS, which was already separately verified): `POST /api/demo/kill-worker` produced two `worker.updated` events, `draining` at 2ms after connect and `offline` at 2095ms — deterministic, not a race. **Deliberately scoped to the demo path only** — the real worker lifecycle (`worker/src/heartbeat.ts`) keeps relying on its own heartbeat-driven publish; adding an explicit publish per state change to the real worker lifecycle was never asked for and would be a meaningfully different (and riskier — more Redis publishes on the real, non-demo path) change. A heartbeat tick landing in the same 2-second window as an explicit publish is harmless and not de-duplicated: the frontend's `worker.updated` handler (`App.tsx`) doesn't apply the event payload directly, it triggers `workers.retry()` (a fresh `GET /api/workers`), so a duplicate event is a redundant refetch of the same current state, not a correctness risk. 2026-08-12.
- **M10 bugfix pass: `FAILURE_CLUSTER_DEFAULT_WINDOW_HOURS` raised from 24 to 168 (7 days), and the failures window made fully user-facing rather than a fixed server default.** The 24h default meant the Failures page could show "no failures" purely because seed data had aged past a fixed trailing window since the last `pnpm db:seed` run (see the ruled-out seed-anchoring entry above) — a false-negative that reads as a bug on every demo run more than a day after seeding. `windowHours` was already validated end-to-end (Zod, `.min(1).max(720)`) on both `GET /api/failures/clusters` and `POST /api/ai/diagnose` — only the frontend never exposed it. Added a 24h/7d/30d selector on the Failures page (`web/src/pages/Failures.tsx`), defaulting to 7d (matching the new server default), threaded through `useFailureClusters` and the diagnose call so the AI summary is always scoped to the exact same window the clusters on screen are — previously `runDiagnose()` called `diagnoseFailures()` with no params at all, silently using the diagnose endpoint's own default rather than whatever window the clusters card happened to be showing, so the two could disagree. The empty state also now distinguishes "no failures in this window" (shown when a wider window would still be empty, i.e. this is a genuinely quiet system) from a shorter, cheaper phrasing implying a narrower window might have missed something — previously one generic empty-state string covered both cases. 2026-08-12.
- **M10: heartbeat-freeze bug fixed, root cause never definitively confirmed — closed via a timeout+retry wrapper plus connection diagnostics, verified clean under the exact conditions that reproduced it twice before.** Owner's explicit call (Option 1) rather than a further isolated diagnosis phase: `worker/src/lib/with-db-timeout.ts` wraps the heartbeat write (5000ms) and the task-logger write (10000ms, same fire-and-forget exposure) in a timeout race plus up to 2 retries (1s/3s backoff), logging label/elapsed/attempt on every timeout so a stall is loud instead of silent; the heartbeat interval itself still never throws. Both `worker/src/db/client.ts` and `server/src/db/client.ts` also gained `idle_timeout: 60`, `connect_timeout: 10`, and an `onclose` log line — cheap diagnostics matching the leading suspect (Neon/Upstash silently dropping an idle connection) without a separate investigation phase. Verified live against the real Neon+Upstash stack: triggered `postgres-nightly-backup` (the same 221-second job used in the original two freeze reproductions) via the new `/api/demo/trigger`-adjacent manual-trigger path; the claiming worker logged 44 consecutive heartbeat tick pairs over the full run with every `starting DB write` matched by a `DB write returned` within ~85-100ms, no gap, task succeeded. This matches — but does not exceed — the earlier clean 13-minute/360-tick run already on record before this fix existed, so a single clean run here is consistent with the fix working but is not proof the intermittent freeze can no longer occur; the wrapper's real job is turning a future recurrence into a loud, retried, self-recovering event instead of a silent one. 2026-08-11.
- **M10: kill-worker demo endpoint mutates DB state only — never touches an OS process.** Owner's explicit call after flagging that the server has no channel to a live worker process (draining/shutdown only ever runs from inside `worker/src/shutdown.ts`, triggered by a local SIGTERM/SIGINT on that same process). `POST /api/demo/kill-worker` (`server/src/services/demo.service.ts`) picks the online worker with the highest `inflight` (any online worker if all are idle; 409 `NO_ONLINE_WORKER` if none are online), sets `status: 'draining'` immediately, then after a 2s delay backdates `last_heartbeat_at` to 30s in the past so the next read reports it `offline` per the existing 15s staleness rule — no code changes to that rule. Any run the worker was holding is deliberately left alone; BullMQ's own stalled-job recovery (already built and tested at M6) is what picks it back up, which is the actual point of the button. `POST /api/demo/reset` restores every worker row to `status: 'online'` with a fresh heartbeat. The Overview demo strip's "Kill a worker" button carries a tooltip stating this plainly. Verified live: killed `worker-36` (had `inflight: 1` from a just-fired demo trigger, correctly picked over idle workers), confirmed `draining` immediately; its own real heartbeat loop (still running, never actually killed) kept refreshing `last_heartbeat_at` every 5s after that, which correctly kept it out of the `offline` state my backdate would otherwise have forced — i.e. a genuinely alive worker cannot be forced to read `offline` past its own next real heartbeat, only a truly stalled/crashed one would stay backdated. `demo/reset` restored it to `online` cleanly.
- **M10: demo/trigger and demo/break target two specific already-seeded jobs by id, rather than picking randomly or accepting a jobId param.** `competitor-pricing-scrape` (short duration, `failureMode: 'none'`) is the "Fire a job" happy-path target; `stripe-webhook-reconcile` (already seeded with `failureMode: 'rate_limit'`) is the "Break something" target — chosen because `worker/src/worker.ts` reads `taskInput` from the job row itself, not per-run, so there is no way to inject a one-off failure mode without either mutating a real job's stored config (rejected — a demo action permanently changing a job's config is a worse trade than a fixed target) or seeding a dedicated demo-only job (not asked for, adds a 7th job to a fleet PHASE2.md describes as six). Both ids are hardcoded constants in `demo.service.ts`; if either job is ever renamed or deleted, `demoTrigger`/`demoBreak` throw a clear `DEMO_JOB_MISSING` 409 rather than failing silently.
- **M10: rate-limit middleware generalised into a `createFixedWindowRateLimit` factory, shared by `aiRateLimit` and the new `demoRateLimit`.** Previously `middleware/rate-limit.ts` hardcoded one AI-specific limiter. The demo panel (§10) needed its own per-IP fixed-window limit with independent numbers (`DEMO_RATE_LIMIT_PER_MIN=10`, `DEMO_RATE_LIMIT_WINDOW_MS=60000`, both env-tunable per the same reasoning already applied to the AI limits) and its own counter map, so a demo burst can never eat into the AI quota or vice versa. Extracting the shared fixed-window logic into a factory was a same-turn generalization with a second real call site already in hand, not speculative — `aiRateLimit`'s behavior and error code/message are unchanged.
- **M10: `/api/demo/*` guarded by per-route `preHandler: [demoModeGuard, demoRateLimit]`, not `app.addHook` inside `demo.routes.ts`.** `server/src/routes/index.ts` calls every `register*Routes(app)` directly on the same top-level Fastify instance rather than via `app.register(plugin)` encapsulation (confirmed by reading `app.ts`/`routes/index.ts` before writing the route file) — an `app.addHook('preHandler', ...)` call inside any one route module would silently leak that hook onto every route registered afterward, not just its own. Matches the existing `ai.routes.ts` pattern (`{ preHandler: aiRateLimit }` per route) rather than introducing a new registration style.
- **M10: Failures page's "Show raw logs" toggle removed, not ported to a real data source.** It displayed `mockData.RAW`, eight hardcoded fake log lines with no backing endpoint (§6 only offers `/api/runs/:id/logs` for one specific run, not an all-jobs raw feed) — deleting `mockData.ts` left it with nothing real to show. Owner's explicit call (over relabeling failure-cluster sample messages as "raw logs", rejected as misleading): removed entirely — button, panel, and the `RAW`/`showRaw`/`rawLog`/`rawTitle`/`rawLabel`/`onToggleRaw` props all deleted from `Failures.tsx` and `App.tsx`. The right-hand card is now permanently "Suggested fixes", populated from `/api/ai/diagnose`'s real `fixes` array (empty-state prompts the owner to run a diagnosis). Real per-run logs remain exactly where they already worked correctly: the Job Detail page's live log stream via `/api/runs/:id/logs`. Removed-not-deferred — this is not a Phase 3 candidate; if a genuine cross-job raw log view is ever wanted, it needs a real spec'd endpoint, not a revival of this mock.
- **M10: `/api/ai/diagnose` and its Failures-page button are on-demand only, not auto-fired on page load.** Owner's explicit call, matching the Composer page's explicit-submit pattern and §9.2.6's rate-limit concern — clusters (deterministic, free) still load automatically as before; the Gemini-backed diagnosis only runs when "Diagnose failures" is clicked. Result is cached in component state (`App.tsx`'s `diagnosis`/`diagnosisLoading`/`diagnosisError`) so re-opening the Failures page or re-clicking within the same session doesn't re-fire the call — a full page reload does, which is consistent with every other piece of client-side state in this app (nothing here persists across reloads, e.g. `composedConfig` on the Composer page behaves identically).
- **M10: diagnose validator strips `clusters` from what Gemini's output is checked against — the service fills it in from the deterministic cluster data already computed in code.** `aiDiagnoseResponseSchema` (shared, §6's full response shape) includes `clusters: FailureCluster[]`, but the model is never asked to produce it — `DIAGNOSE_FAILURES_RESPONSE_SCHEMA` (the structured-output schema actually sent to Gemini) only requests `summary`/`findings`/`fixes`, and `server/src/ai/validators/diagnosis.validator.ts` validates against that same reduced shape. Matches §9.4 step 2's own instruction that clustering happens in code — asking Gemini to also echo back cluster data it didn't compute would be pure hallucination risk with no upside, since the service already has the authoritative cluster array in hand from `getFailureClusters`.
- **Post-M10 hardening: `demo.service.ts`'s two direct `db.update(workersTable)...` calls moved into `worker.repository.ts` as `setWorkerStatus`/`backdateWorkerHeartbeat`/`restoreWorkerHeartbeat`.** Flagged during a demo-reset idempotency pass: §12.1 says a service holds business rules and never raw SQL, a repository holds queries — `demo.service.ts`'s original `demoKillWorker`/`demoReset` imported `db`/`workersTable`/`eq` directly, violating that split (and, practically, made the service impossible to unit test without a real database connection, since nothing existed to mock at the repository boundary). Fixed as a same-turn correction, not deferred — the resulting `demo.service.test.ts` mocks only `worker.repository.ts` (plus `job.repository.ts`/`run.repository.ts`/`queue/job.queue.ts`, transitively reached via `enqueue.service.ts` for `demoTrigger`/`demoBreak`), the same pattern `enqueue.service.test.ts` already established. 2026-08-11.
- **`POST /api/demo/reset` made genuinely idempotent: it now diffs each worker's current row against "already restored" before writing, and returns `{restoredWorkerIds, workers}` instead of a bare worker array.** Previously every call unconditionally overwrote every worker row with a fresh timestamp, which was harmless in effect (repeated calls converge on the same end state) but reported nothing about whether it actually did anything — indistinguishable, from the response alone, between "fixed 3 stuck workers" and "fleet was already clean." `demoReset` (`server/src/services/demo.service.ts`) now treats a worker as already-clean when `status === 'online'` and its heartbeat is under 1 second old, skips the write for those, and reports the ids it actually touched. Never throws and always returns 200 regardless of fleet state — an empty `restoredWorkerIds` array on a clean fleet is success, not a no-op error. **Job config and paused-job restoration were explicitly scoped out**, not omitted by oversight: no demo action (`demoTrigger`, `demoBreak`) mutates a job's stored config or its pause state — `demoBreak` fires `stripe-webhook-reconcile` exactly as seeded, by design (see the M10 entry above on why config mutation was rejected) — so there is nothing of that kind for a reset to produce. A one-line comment sits directly above `demoReset` recording this so a future reader doesn't mistake the absence for a gap. Covered by `server/src/services/demo.service.test.ts`: reset on an already-clean fleet, reset immediately after a simulated `demoKillWorker` state, and reset called twice in a row (second call finds the fleet the first call already fixed and restores nothing). 2026-08-11.
- **INCIDENT: ~10 duplicate long-lived `server`/`worker` dev processes (6 server, 4 worker) were found running simultaneously against the same live Neon+Upstash stack, most predating this session — the actual dominant cause of a real Redis command-budget spike the owner flagged mid-session (reported ~8 cmd/sec, risking free-tier exhaustion within the hour).** Each stray process ran its own BullMQ `Worker` instance(s) at default polling settings, so the fleet's effective idle Redis command rate was whatever one process's idle floor is, multiplied by however many duplicates happened to still be alive — not a single process's polling being too aggressive. Root cause: repeated `tsx watch` invocations across this session's live-verification passes (M10's heartbeat and demo-panel testing) were started as new background processes without confirming prior ones had exited; `tsx watch`'s file-watching keeps a process alive indefinitely with nothing to naturally terminate it. All 27 stray node processes (dev servers, workers, and one vite instance) were force-stopped immediately upon the owner's confirmed instruction, before any further diagnosis or code changes. No code defect caused this — it is a session/process-hygiene gap, not a bug in `worker.ts`/`schedule-tick.worker.ts`. Mitigated going forward by the Redis client census log line (see below) making a process's expected idle footprint visible at boot, so an unexpectedly-high aggregate is easier to notice while it's happening rather than after the fact. 2026-08-11.
- **BUG FOUND AND FIXED: `startRedisEventSubscriber()` (`server/src/realtime/redis-subscriber.ts`) was fully implemented at M8 but never actually called anywhere — `server/src/index.ts` never invoked it.** Found while auditing whether `QueueEvents` (or any other Redis-connecting client) was being instantiated per SSE connection rather than once per process, per the owner's request — there turned out to be no `QueueEvents` instance anywhere in the codebase (M8 already chose Redis pub/sub over it, per that milestone's own DECISIONS.md entry), but this genuinely-wired-nowhere subscriber was found in the process. **Practical impact: every worker-originated SSE event — `run.started`, `run.log`, `run.finished`, `worker.updated` — has never actually reached the browser**, since those are published by the worker process onto the `flowforge:events` Redis channel and only `startRedisEventSubscriber()` bridges that channel onto the server's in-process event bus that `sse.handler.ts` fans out from. Only server-originated events (`run.queued`, published directly by `enqueue.service.ts`) were ever live. This directly contradicts M8's own DECISIONS.md entry, which describes this exact bridge as built and "verified live: triggering a run mid-session updated the headline counters immediately on the next tick" — that verification pass evidently exercised `stats.tick` (a real server-originated path) without separately confirming a worker-originated event (e.g. a live `run.log` line appearing) actually arrived, so the gap went unnoticed for two milestones. **Fixed**: `server/src/index.ts` now calls `startRedisEventSubscriber()` at boot (right after `startScheduleTickWorker()`) and `.stop()` in the shutdown handler, alongside the other graceful-shutdown steps. Exactly one subscriber per server process, confirmed by reading `sse.handler.ts`: each individual SSE connection only calls `subscribeToEvents()` against the in-process `EventEmitter`-backed event bus, never touching Redis directly — so this fix does not multiply Redis connections per browser tab, only adds the one subscriber connection that was always supposed to exist per server process. Not yet re-verified live end-to-end in a browser (that pass belongs to the owner per this session's earlier instruction); server-side, the fix is a two-line call-site change with no new logic to unit test. 2026-08-11.
- **Redis command budget tuned down from BullMQ's defaults, applied to every `Worker`/`Queue` instance.** Rationale: on a free-tier hosted Redis (Upstash, in local dev — see below), idle BullMQ polling is a standing cost that scales with wall-clock time and process count, not with actual job volume — a `Worker` sitting idle still issues a blocking-pop cycle roughly every `drainDelay` seconds plus a stalled-job sweep every `stalledInterval` ms, forever, regardless of whether any job is ever enqueued. BullMQ's defaults (`drainDelay: 5`, `stalledInterval: 30000`) are tuned for responsiveness on a self-hosted/unmetered Redis, not command-count economy on a metered free tier. Changed, as shared constants in `packages/shared/src/constants/queue.ts` (`QUEUE_DRAIN_DELAY_SECONDS`, `QUEUE_STALLED_INTERVAL_MS`) so every `Worker` instance — the job worker (`worker/src/worker.ts`) and the schedule-tick worker (`server/src/queue/schedule-tick.worker.ts`) — uses the same numbers: `drainDelay: 60` (was 5 — a 12x reduction in idle blocking-pop frequency) and `stalledInterval: 60000` (was 30000 — the owner's explicit instruction not to raise this further, since `POST /api/demo/kill-worker`'s entire point depends on BullMQ's stalled-job recovery actually firing to reclaim a dead worker's in-flight run; raising this interval further would make that recovery — and the demo button built around it — visibly slower without a corresponding command-budget win worth the tradeoff). Also added `QUEUE_DEFAULT_JOB_OPTIONS` (`removeOnComplete: {count: 100}`, `removeOnFail: {count: 500}`) to both `Queue` instances (`job.queue.ts`, `schedule-tick.queue.ts`) — BullMQ keeps completed/failed job data in Redis indefinitely by default, which is itself a standing storage/command cost on every future `Queue` operation that has to look past accumulated history; capping retained job counts bounds that growth without touching the `runs`/`run_logs` Postgres tables, which remain the actual source of truth for run history (§5) — this only prunes BullMQ's own bookkeeping copy. Confirmed no Upstash-specific assumptions exist anywhere in the Redis connection code (`server/src/queue/connection.ts`, `worker/src/queue-connection.ts` both do a plain `new Redis(env.REDIS_URL, {maxRetriesPerRequest: null})` — no hardcoded TLS flags, no Upstash REST API, no assumed hostname format) or in either `.env.example`, so `REDIS_URL` already points anywhere a standard Redis connection string works. 2026-08-11.
- **Phase 3 will self-host Redis on the same AWS EC2 instance as the server/worker (per the existing Phase 3 hosting entry above); Upstash remains local-dev-only for the rest of Phase 2.** Restated here specifically in the context of the Redis command-budget tuning above: the `drainDelay`/`stalledInterval` values were chosen conservatively because Upstash's free tier prices by command count, a constraint that disappears once Redis is self-hosted on EC2 (commands become free relative to the instance's own compute, not metered). Phase 3 may reasonably loosen `QUEUE_DRAIN_DELAY_SECONDS` back toward BullMQ's more responsive defaults once that migration happens, trading the now-irrelevant command-count concern back for lower job-pickup latency — but that is a Phase 3 decision, not made now. Nothing about today's REDIS_URL/connection code needs to change for that migration; only the tuning constants might. 2026-08-11.
- **Heartbeat fix (earlier M10 entry above): restated for clarity — root cause was never conclusively identified, only narrowed to a leading suspect (Neon/Upstash silently dropping an idle connection) that was never directly proven or disproven.** The fix (`withDbTimeout` retry wrapper + connection diagnostics) does not depend on the root cause being correct to be effective: it converts *any* silent hang in the heartbeat or task-logger write — whatever its actual cause — into a logged, retried, self-recovering event instead of an invisible one. A single clean 221-second live run after the fix (see the original entry) is consistent with the fix working but does not, by itself, prove the underlying freeze can no longer occur; the wrapper's guarantee is about what happens *if* it recurs, not that it won't.
- **CORRECTION to the "Redis command budget tuned down" entry above: `QUEUE_DRAIN_DELAY_SECONDS = 60` did not, and structurally cannot, reduce the idle `BZPOPMIN` cadence to 60s — the earlier entry's framing was wrong.** Caught via the owner's own live Redis MONITOR capture, which showed `BZPOPMIN "bull:flowforge-schedule-ticks:marker" "10"` — a 10-second timeout — despite `drainDelay: 60` being correctly present in the `Worker` constructor's options object (confirmed: no shared/default options object merges over it afterward; `Object.assign({drainDelay: 5, ...}, opts)` inside BullMQ's own `Worker` constructor correctly resolves `this.opts.drainDelay` to `60`). Root cause, found by reading `bullmq@5.81.3`'s own source (`dist/esm/classes/worker.js`, `getBlockTimeout()`): `drainDelay` is only consulted when a queue has **zero** delayed/scheduled jobs pending (the `else` branch, `Math.max(opts.drainDelay, minimumBlockTimeout)`). The instant any delayed job exists — a job queue with a retry-backoff delay in flight, or the schedule-tick queue, which **always** has one, since every cron job is a BullMQ Job Scheduler with a permanent "next slot" delayed job — BullMQ takes the other branch and computes `Math.min(blockDelay / 1000, maximumBlockTimeout)`, where `maximumBlockTimeout` is a hardcoded, non-configurable module-level constant equal to `10` (their own comment: "10 seconds is the maximum time a BZPOPMIN can block... to avoid blocking the connection for too long in the case of reconnections," citing `taskforcesh/bullmq#1658`). `drainDelay` is never read in that branch at all. **Practical effect: on both queues in this app, the real idle `BZPOPMIN` cadence has always been BullMQ's fixed 10s ceiling, not the 60s this codebase's constant implied** — the earlier entry's "12x reduction in idle blocking-pop frequency" claim was therefore false; the actual reduction from BullMQ's own un-capped default (`drainDelay: 5`, which is irrelevant here for the same reason) never happened, because there was never a lever to pull. `QUEUE_DRAIN_DELAY_SECONDS` is left at `60` (harmless, and correct for the rare case a queue is ever genuinely delayed-job-free) but its doc comment in `packages/shared/src/constants/queue.ts` now states this ceiling explicitly, and both processes' boot-time Redis client census log lines now report `effectiveBzpopminIntervalSeconds: 10` alongside the configured (and largely inert) `drainDelaySeconds`, so this isn't rediscovered the hard way again. No BullMQ-supported way exists to raise `maximumBlockTimeout` itself. 2026-08-11.
- **Each `BZPOPMIN` wakeup was undercounted as one Redis command — it's actually a burst of ~7 (`EVALSHA`, `HMGET`, `ZRANGEBYSCORE`, `RPOPLPUSH`, `ZPOPMIN`, `DEL`, `ZRANGE`), per the owner's own MONITOR trace.** Every non-empty poll cycle runs BullMQ's `moveToActive`-family Lua script plus its own bookkeeping reads, not a single call — the correct unit of idle cost is *bursts*, not raw blocking calls. Combined with the corrected ~10s cadence above (not 60s), this means **idle uptime, not job volume, is the dominant Redis command-budget cost for this app**: two processes (server, worker) each running one polling `Worker`, each producing a ~7-command burst roughly every 10 seconds, forever, whether or not anything is ever enqueued. Over a full day of two idle processes left running: roughly `2 processes × 6 bursts/min × 60 min × 24 hr × 7 cmd/burst ≈ 121,000 commands/day` from BZPOPMIN bursts alone, before counting heartbeat publishes, stalled-job sweeps, or any real job traffic — a number that scales with wall-clock uptime and process count, exactly as the incident that prompted this pass demonstrated (10 duplicate long-lived dev processes, not job volume, produced the ~8 cmd/sec spike). The `pnpm redis:floor` script (`server/scripts/redis-floor.ts`, added this pass) measures this directly against a running pair: 30s settle window to skip boot-time bursts, 90s `MONITOR` capture, reports total commands/cmd-per-sec plus a per-burst-type breakdown (commands within 200ms grouped as one burst) — kept as a permanent tool for re-measuring after any future queue/worker change, not deleted after use. 2026-08-11.
- **Heartbeat `worker.updated` Redis `PUBLISH` was firing unconditionally on every `HEARTBEAT_INTERVAL_MS` tick (every 5s, forever) even when nothing about the worker had actually changed — an avoidable, steady Redis command cost, separate from the `BZPOPMIN` floor above.** Fixed in `worker/src/heartbeat.ts`: the Postgres write (`last_heartbeat_at`/`inflight`) still happens on its full, unchanged `HEARTBEAT_INTERVAL_MS` cadence — that write is what `worker.service.ts`'s staleness check reads to decide online/offline, so slowing it down would make that check less accurate, and the owner explicitly required the two cadences stay decoupled. Only the Redis `PUBLISH` (which exists purely to push a live UI update, not to keep the worker "alive" in any read path) is now gated: it fires immediately if `status` or `inflight` differs from the last thing actually published, and otherwise at most once every 30 seconds regardless — so a viewer's screen still refreshes periodically even during a fully idle stretch, but a worker sitting idle with unchanged state no longer publishes 12 identical payloads a minute. `lastHeartbeatAt` is deliberately excluded from the change comparison (it differs on literally every tick by definition, so including it would defeat the whole gate). 2026-08-11.
- **Local dev now runs against a self-hosted Redis (Memurai on Windows — confirmed running as a service, PONG-verified by the owner), fully off Upstash — Upstash is reserved for deployed environments only, and this session's incident (10 duplicate dev processes plus two real polling defects driving real Upstash command spend) is exactly why.** `REDIS_URL` was already fully env-driven with zero Upstash-specific assumptions anywhere in the Redis-connecting code (`server/src/queue/connection.ts`, `worker/src/queue-connection.ts` — both a plain `new Redis(env.REDIS_URL, {maxRetriesPerRequest: null})`, no hardcoded TLS requirement, no `rediss://` assumption, no `@upstash/redis` import anywhere in the BullMQ path, confirmed by reading both files again this pass), so pointing local dev at `redis://127.0.0.1:6379` instead of a hosted Upstash URL was a pure `.env` change: `server/.env` and `worker/.env` now both read `REDIS_URL=redis://127.0.0.1:6379`, and `GET /api/health` confirmed `redis: {ok: true}` against it live. `server/.env.example`, `worker/.env.example`, and `SETUP.md`'s Redis section were updated to make local self-hosted Redis the primary documented path and reframe Upstash as deployed-only. Phase 3 self-hosts Redis on the same AWS EC2 instance as the server/worker (per the existing Phase 3 hosting entry above) — local dev moving off Upstash now is the same direction that migration already goes, just arriving early for local dev specifically, for cost/quota-safety reasons rather than architecture reasons. 2026-08-11.
- **Measured idle Redis command floor, live against local Redis: 1.089 cmd/sec (98 commands over a 90s capture, one clean server + one clean worker, zero jobs running) — confirmed via `pnpm redis:floor`.** Burst breakdown from that run: the dominant burst type was the 8-command `BZPOPMIN` cycle (`EVALSHA+HMGET+ZRANGEBYSCORE+RPOPLPUSH+ZPOPMIN+DEL+ZRANGE+BZPOPMIN`, one command more than the 7 seen in the owner's earlier Upstash trace — this run's captured cycle also included the leading `EVALSHA` for the stalled-job-check script that the earlier trace's window happened to miss), firing 9 times in 90s (~6/min, matching the ~10s BullMQ-hardcoded ceiling documented above) for 72 of the 98 total commands; the gated heartbeat `PUBLISH` fired 3 times in 90s (~2/min, i.e. roughly once per 30-45s) confirming the throttle fix holds — down from the old unthrottled ~18 times in 90s a 5s-interval heartbeat would have produced; the remaining commands were a one-time 7-command stalled-check burst and a one-time 15-command combined burst (both boot-adjacent, still inside the 90s window but not a repeating idle pattern) plus the script's own final `QUIT`. **This confirms the revised goal directly: 0.25 cmd/sec was never reachable given BullMQ's hardcoded 10s `BZPOPMIN` ceiling and ~7-8 commands per wakeup across two queues — ~1.1-1.4 cmd/sec is the real, structural idle floor for this app's current two-Worker-process shape, and no further BullMQ-level tuning changes that.** The reasoning that follows from this: since Phase 3 self-hosts Redis on the same EC2 instance as the server/worker (commands become free relative to the instance's own compute, not metered) and local dev now runs against a free self-hosted Redis, this floor costs nothing in either environment where it will actually run long-term — the only environment where it was ever a real cost was Upstash's metered free tier, which local dev no longer uses. See the entry below for what this floor would have cost against Upstash's actual limit, for the record. 2026-08-11.
- **Projected monthly cost of the measured 1.089 cmd/sec idle floor against Upstash's 500,000-command/month free tier, had local dev kept running there 24/7: `1.089 × 86,400s/day ≈ 94,090` commands/day, `× 30` ≈ **~2.82 million commands/month — about 5.6× the entire free-tier allotment**, meaning the tier would be exhausted in roughly `500,000 / 94,090 ≈ 5.3 days` of continuous idle uptime alone, with zero jobs ever run.** This is not a hypothetical: it is a close match to the actual incident that started this investigation (10 duplicate long-lived dev processes, most left running unintentionally across a session, produced an observed ~8 cmd/sec — consistent with roughly 5-7 of that same ~1.1-1.4 cmd/sec idle floor being multiplied by simultaneous duplicate processes, not by unusually heavy polling settings). The number that matters going forward is uptime × process count, not job volume — a point already made above but worth restating with the actual dollar-relevant comparison: at this floor, *merely leaving one dev server and one dev worker running* would have burned the entire monthly Upstash free-tier budget in under a week, regardless of whether the app was ever used. This is the concrete argument for why local dev now self-hosts Redis (previous entry) and why Phase 3 self-hosts Redis on EC2 rather than metering production against a hosted free tier (existing Phase 3 hosting entry) — both environments where this floor will actually run for extended periods needed to stop depending on a per-command-metered service. 2026-08-11.
- **Single-instance lock added: a worker (job worker or the server's schedule-tick worker) binds no port, so nothing at the OS level ever signaled that 10 duplicate processes were running during the incident that started this whole investigation — no port conflict, no crash, no error, just quietly multiplied command spend.** `packages/shared/src/constants/instance-lock.ts` defines the key namespace (`flowforge:instance-lock:<kind>:<identity>`) and shared tuning (`INSTANCE_LOCK_TTL_MS = 90000`, `INSTANCE_LOCK_REFRESH_INTERVAL_MS = 60000` — TTL deliberately long relative to refresh cadence, since the lock's job is to catch a duplicate *process*, not to detect a dead one quickly; that's what the existing 15s heartbeat-staleness check in `worker.service.ts` already does). On boot, each process runs `SET key value PX 90000 NX` (`worker/src/instance-lock.ts`, `server/src/lib/instance-lock.ts`); a conflict throws `InstanceLockConflictError`, which both entry points (`worker/src/index.ts`, `server/src/index.ts`) catch, log at `error`, and `process.exit(1)` on — the process never gets far enough to register itself, claim jobs, or open its polling connections. The lock is refreshed via a Redis Lua script (`if GET key == myToken then PEXPIRE ... else 0`, never a bare `PEXPIRE`, so a lock this instance no longer actually owns — because its previous refresh was missed long enough for another instance to reclaim it — can't be silently re-extended out from under the new owner) — on the worker's existing 5s heartbeat tick but gated to only actually fire once per `INSTANCE_LOCK_REFRESH_INTERVAL_MS` (no new timer, no added interval), and on a small dedicated interval for the server (which has no other natural per-instance cycle to piggyback on). Released the same way on graceful shutdown (same GET-then-DEL script), and self-heals via TTL if a process is force-killed and never gets to run its shutdown handler at all — verified live: a force-killed test instance's lock was still held for the remainder of its 90s TTL, then a fresh instance was able to boot cleanly once it expired, with zero manual intervention. 2026-08-11.
- **The lock's identity is deliberately separate from a worker's cosmetic `worker-NN` display id (`registration.ts`) — this distinction is what actually closes the incident's root cause, and an earlier version of this feature (see the superseded paragraph below) got it wrong.** First implementation locked on the auto-assigned `worker-NN` display id itself. Live testing immediately exposed the gap: with `WORKER_ID` unset (the default), `resolveWorkerId()` assigns a *fresh* `worker-NN` on every boot (`worker-44`, then `worker-45`, ...), so two duplicate `pnpm dev` runs — exactly the incident's actual failure mode — got two different display ids and therefore two different, non-colliding locks. The lock worked perfectly for its literal spec ("no two processes can hold the same identity") while doing nothing to prevent the actual duplicate-process problem it was built for. Fixed by splitting lock identity from display identity (`resolveLockIdentity()` in `worker/src/instance-lock.ts`): if `WORKER_ID` is explicitly set, the lock identity IS that value (explicit intent wins — this is also what M7-era horizontal worker replicas will rely on, to run several distinctly-`WORKER_ID`'d workers side by side without colliding). If `WORKER_ID` is unset, the lock identity is instead a derived, *stable-across-restarts* fingerprint: `sha256(hostname + this checkout's absolute repo path + the job queue name)`, truncated to 16 hex chars and prefixed `derived-`. This is stable for "the same machine, the same checkout, restarted" (exactly the incident's scenario — repeated `pnpm dev` with no `WORKER_ID` set, same laptop, same folder) while correctly staying distinct for "a different machine" or "a different checkout on the same machine" (legitimate parallel work, e.g. two feature branches checked out to two folders — confirmed analytically: the repo-path component of the hash differs, so the resulting 16-hex-char identities differ, by SHA-256's own collision resistance, without needing a second full `pnpm install`'d checkout to prove it at runtime). The rejection message always names which identity collided and states plainly whether it was explicit or derived, so a developer hitting this never has to guess why. 2026-08-11.
- **Live verification, all four required cases, against the corrected derived-identity lock:** (1) same machine, `WORKER_ID` unset, started twice — second instance rejected, exit 1, message names the `derived-...` identity and says plainly that unset-`WORKER_ID` instances on the same machine+checkout always collide by design. (2) same machine, explicit `WORKER_ID=worker-99` twice — second instance rejected, exit 1, message names `worker-99` as the explicit collided identity. (3) same machine, explicit *different* ids (`worker-99`, then `worker-98`) — both booted successfully alongside each other and the still-running unset-id instance, all three counted correctly by the boot-census `liveWorkerInstances` field (1 → 2 → 3 as each joined). (4) different repo path, `WORKER_ID` unset — verified analytically rather than via a second full checkout+install (unnecessary given SHA-256's collision resistance and that the formula, not runtime behavior, is what's actually being checked): the derived-identity formula run against two different absolute paths on the same hostname produces two distinct 16-hex-char identities (`derived-9827db03151b5973` vs `derived-dc5f81f82c985253`), confirming two checkouts never collide. 2026-08-11.
- **The lock's own Redis command cost, measured before/after via `pnpm redis:floor`: no meaningful change to the idle floor, as expected.** Before (no lock): 1.089 cmd/sec, 98 commands/90s. After (lock added, `EVAL`-based refresh + boot-time `SET NX` + `countLiveWorkerInstances`'s one-time `KEYS` scan at census): 0.956 cmd/sec, 86 commands/90s — within normal run-to-run variance, not a regression (the difference is explained by one fewer boot-adjacent burst landing inside this run's 90s window, not by the lock adding cost). The lock's own visible contribution: an `[EVAL+GET+PEXPIRE]` burst (the refresh script) fired 2 times in the 90s capture — 1.33/min, matching the owner's own back-of-envelope estimate of "roughly 2 commands/min" for a `SET NX PX` on the existing heartbeat cycle almost exactly. `countLiveWorkerInstances()`'s `KEYS flowforge:instance-lock:worker:*` scan (used only in the boot-time census log, never on a repeating timer) is a full-keyspace-pattern scan, generally discouraged in production Redis at scale — acceptable here since it runs once per process lifetime, against a Redis instance whose total key count is small (this app's own working set, not a shared multi-tenant instance), so its cost doesn't compound the idle floor the way a per-tick command would. 2026-08-11.
- **BUG FOUND AND FIXED, first actual run of `pnpm qa` against the merged harness: `reply.raw.writeHead()` alone left the SSE response's headers buffered and never flushed to the socket — every client (curl, Node's `fetch()`, Chromium/Playwright's `EventSource`) saw zero bytes for up to `SSE_HEARTBEAT_MS` (20s), far past any reasonable client-side connect timeout.** Reproduced directly, isolated from the app: a fresh server process, confirmed via debug logging that `handleSseConnection` entered, read headers, and returned from `writeHead()` in milliseconds — but `curl --max-time 5` against the same live connection received 0 bytes. Adding one `reply.raw.write(': connected\n\n')` immediately after `writeHead()` fixed it instantly — confirmed live, same curl call, headers and the comment line both arrived within the connection's first round-trip. This was the reason `pnpm qa`'s SSE checks failed on its first run despite the CORS fix (an earlier session's entry above) being independently correct and already verified via `fetch()` — that earlier verification happened to catch enough bytes across a longer-running probe to see the header, but a shorter, more realistic client timeout (curl's default, a browser's own EventSource timeout) would have failed exactly as `pnpm qa` did. **This is a third, previously-undiscovered way the SSE path could look "fixed" under one verification method while still being broken for a real client** — see the standing "browser verification required" entry below, which this reinforces rather than replaces: this bug specifically needed a client with a real, human-scale timeout, which neither `fetch()`'s default (very long) nor the earlier session's specific probe exercised. Fixed in `server/src/realtime/sse.handler.ts` — an explicit initial write immediately after `writeHead()`, with a comment explaining why it's there so it isn't "cleaned up" as dead code later. 2026-08-12.
- **BUG FOUND AND FIXED, same `pnpm qa` run: `startStatsTick()` (`server/src/realtime/stats-tick.ts`) was fully implemented — the exact code that recomputes and publishes the Overview's `stats.tick` payload every `STATS_TICK_INTERVAL_MS` (3s) — but never actually called from `server/src/index.ts`.** The only reference to `startStatsTick` anywhere in the server source was its own definition; nothing imported or invoked it. **Practical impact: the Overview page's live counters and "Recent runs" table have never actually updated without a manual refresh**, since `stats.tick` is the only thing that repoints `App.tsx`'s `liveStats` away from the one-shot `GET /api/stats/overview` fetch (see the M8 `overviewStats = liveStats ?? stats.data` entry above) — a fired job's row, or the headline `runs/success-rate/queue-depth` numbers, would sit frozen at whatever they were on page load, indefinitely, regardless of SSE connection health. This is the exact same shape of bug as the already-documented M8 `startRedisEventSubscriber()` gap above (fully built, never wired up, invisible to any verification that didn't specifically watch for *this* event arriving) — found the same way, by a `pnpm qa` check (`Recent-runs row changes over 25s without a page refresh`) that specifically asserted on a value changing over time rather than just checking the connection succeeded. Fixed: `server/src/index.ts` now imports and calls `startStatsTick()` at boot (alongside the other realtime bridges) and `.stop()` in the shutdown handler, matching the established pattern. Verified live via a direct SSE listener (Node's `fetch()` streaming reader): three consecutive `stats.tick` events at 1726ms, 4684ms, 7702ms after connecting — matching the 3s interval. Re-ran `pnpm qa` after both fixes: all 26 checks passed, including the previously-failing recent-runs-changes-over-time check, now showing a fired job's row genuinely progressing `running` → `succeeded` with real elapsed-time/duration values across three snapshots. 2026-08-12.
- **Three `qa/qa_runner.mjs` checks were themselves buggy (false positives), found and fixed while chasing the two real bugs above — noted so they aren't mistaken for regressions later.** (1) The double-slash-URL check matched on `r.url.includes('//')` against the *full* URL string, which matches the `//` in `http://` on every single request — always a false positive, fixed to parse the URL and check the pathname only. (2) The diagnosis-populated check searched `document.body.innerText` for the literal string `'AI diagnosis'`, but that label is styled `text-transform: uppercase` in `Failures.tsx` — `innerText` reflects the rendered/uppercased text (`'AI DIAGNOSIS'`), not the JSX source string, so the check always failed regardless of whether the diagnosis actually populated; fixed to a case-insensitive match. (3) The loading-label check asserted the diagnose button read exactly `'Diagnosing…'` at a fixed 250ms after clicking, but a cached diagnosis (same window queried earlier in the same run) can resolve faster than that — fixed to accept either the loading label or the already-resolved label, still failing on anything else (a stuck or broken button). 2026-08-12.
- **Added four pre-deployment items to Phase 3 scope (§16 of `PHASE2.md`, still "do not start" — this only expands what the eventual deployment step must cover, not a start signal).** Owner's explicit additions: (1) an AWS Budgets alert at a $1 threshold plus a CloudWatch billing alarm as a second, independent layer — two different alerting paths so one misconfiguration doesn't silently remove all cost visibility; (2) Redis on EC2 configured with `maxmemory` and `allkeys-lru` eviction, sized against the instance's actual RAM, rather than left at Redis's unbounded-growth default; (3) the server and worker run as systemd services with restart limits instead of `pnpm dev` — systemd's own process tracking (a service is either running under systemd or it isn't; a hard-killed or crashed unit is systemd's problem to notice and restart, not an invisible orphan) is what actually prevents the class of incident that already happened locally (10 duplicate long-lived `pnpm dev` processes, none tracked by anything, silently multiplying Redis command spend — see the 2026-08-11 incident entry above); (4) the single-instance Redis lock (already built and verified against manually-restarted local `pnpm dev` processes) needs to be re-confirmed specifically against systemd's own restart behavior (`Restart=on-failure`-style respawns, `RestartSec` timing) before Phase 3 can trust it under the actual process supervisor it will run under — the lock's TTL-based self-heal (documented in the instance-lock entries above) was designed and tested against a bare `Stop-Process`-equivalent kill, not against a supervisor that immediately restarts the process.
- **Self-hosted Redis on EC2 removes command-count metering as a cost risk entirely (see the Upstash free-tier-exhaustion entries above — that risk model doesn't exist once Redis isn't billed per command), but introduces three residual risks worth tracking instead, none of which are about command volume.** (1) **Data transfer out** — AWS bills egress from EC2 by the GB past a small monthly free allowance; a self-hosted Redis instance's own traffic is internal (no egress cost), but the server/worker's own outbound traffic (API responses to the deployed web frontend, Gemini API calls) is a real, separate line item that command-count thinking doesn't capture. (2) **Free-tier expiry at 12 months** — the EC2 free-tier instance this plan assumes is time-limited, not permanent; whatever instance size and Redis config gets chosen needs to keep working (or have a known upgrade path) once that credit period ends, not just during it. (3) **RAM exhaustion on a 1GB instance** — a `t2.micro`/`t3.micro`-class free-tier instance has roughly 1GB of RAM total, shared between the OS, Node (server + worker, both resident simultaneously), and Redis's own dataset; without the `maxmemory`/`allkeys-lru` config from the item above, an unbounded Redis dataset (BullMQ's own job-history bookkeeping, e.g.) growing on the same box that's also running two Node processes is a real out-of-memory risk that command-budget monitoring alone would never surface, since it's a RAM ceiling, not a Redis-command-count ceiling. 2026-08-12.
